<h2><%= @task.title %></h2>
<p class="details"><%= @task.details %></p>

<input type="checkbox" name=" completed">
    <label for="completed"><%= @task.completed ? "This task is completed" : "this task is not completed" %></label>


</br>
<%= link_to "back to list", tasks_path  %>

<%= link_to "Edit this task", editTask_path(@task)  %>

<%= link_to "delete this task", task_path(@task), method: :delete  %>
